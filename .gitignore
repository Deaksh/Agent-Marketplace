__pycache__/
*.py[cod]

# Local DB files (MVP uses SQLite by default)
*.db
oel.db

# macOS
.DS_Store

# Local secrets — keep out of git (copy from `.env.example`). Do not `git add .env`.
.env
.env.local
